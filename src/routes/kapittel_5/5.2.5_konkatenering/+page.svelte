<script>
	let tall_som_tekst = "02"
	let tall = 2
 </script>
 <h2>Konkatenering (tekst som legges etterhverandre)</h2>
<p>{tall_som_tekst + tall_som_tekst}</p>
<p>{tall + tall}</p>
<p>{tall_som_tekst + tall}</p>
<h2>Number(verdi) gjør om til tallverdi</h2>
<p>{Number(tall_som_tekst) + tall}</p>
<p>{Number("Tekst som ikke blir tall") + tall}</p>
